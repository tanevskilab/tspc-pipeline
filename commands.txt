# Main command:

$ nextflow run main.nf -c nextflow.config -profile apptainer

    python -m cellpose --use_gpu \
        --image_path $image \
        --pretrained_model /gpfs/bwfor/work/ws/hd_dy329-tspc/tspc-pipeline/cpsam_20250915_7 \
        --save_tif \
        --verbose


export HOME=\$PWD
export NUMBA_CACHE_DIR=\$PWD